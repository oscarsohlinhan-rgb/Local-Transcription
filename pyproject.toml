[project]
name = "whisperflow-local"
version = "0.1.0"
description = "Local-first lecture transcription UI powered by Faster-Whisper"
requires-python = ">=3.9"
dependencies = [
  "faster-whisper==1.2.1",
  "Flask>=3.1,<4",
]

[tool.ruff]
line-length = 100
